📚 Personal Library API

REST API для управления личной библиотекой пользователя.

Проект позволяет:

создавать и редактировать книги

отмечать книги как прочитанные

добавлять заметки к книгам

работать через JWT-аутентификацию

тестировать API через Swagger

🚀 Технологии

Python 3.10

Django 5.2.9

Django REST Framework 3.16.1

JWT — djangorestframework_simplejwt

Swagger — drf-yasg

SQLite

Pytest

Docker

⚙️ Запуск проекта (Docker)
1. Клонировать репозиторий
git clone <repo_url>
cd my_library-main
2. Запустить проект
docker compose up --build

После запуска сервер будет доступен по адресу:

http://localhost:8000
📖 Swagger документация

Доступна по адресу:

http://localhost:8000/swagger/

Через Swagger можно:

просматривать все endpoints

отправлять запросы

тестировать API без Postman

🔐 Авторизация (JWT)

Используется djangorestframework_simplejwt.

Получить токен

POST запрос:

/api/token/

Body:

{
  "username": "your_username",
  "password": "your_password"
}

Ответ:

{
  "access": "ACCESS_TOKEN",
  "refresh": "REFRESH_TOKEN"
}
Обновить токен
/api/token/refresh/
Использование токена

В заголовке запроса:

Authorization: Bearer <access_token>
📚 API Endpoints

Все endpoints находятся под префиксом:

/api/
Книги
Метод  Endpoint  Описание
GET  /api/books/  список книг пользователя
POST  /api/books/  создать книгу
PUT  /api/update_books/<int:pk>/  обновить книгу
POST  /api/mark_read/<int:pk>/  отметить книгу как прочитанную
Заметки
Метод  Endpoint  Описание
POST  /api/create_notes/  создать заметку
🧪 Тестирование

Проект использует:

pytest

pytest-django

Тестируется сервисный слой:

test_book_services.py

test_note_services.py

Запуск тестов:

pytest
🧠 Архитектура проекта

Проект разделён на несколько слоёв:

Models

Бизнес-сущности:

Book

Genre

Note

MyUser (кастомная модель пользователя)

Service Layer

Бизнес-логика вынесена в:

book_service.py

note_service.py

Это упрощает тестирование и снижает связанность кода.

API Layer

serializers

API views (DRF)

JWT-аутентификация

🐳 Docker

Проект запускается в одном контейнере:

Django backend

SQLite используется как база данных

👨‍💻 Назначение проекта

Учебный backend-проект для практики:

работы с Django и DRF

JWT-аутентификации

сервисного слоя

тестирования

контейнеризации
