📚 Personal Library API

REST API для управления личной библиотекой пользователя.

Проект позволяет:

📖 Создавать и редактировать книги

✅ Отмечать книги как прочитанные

📝 Добавлять заметки к книгам

🔐 Работать через JWT-аутентификацию

📑 Тестировать API через Swagger

🚀 Технологии

Python 3.10

Django 5.2.9

Django REST Framework 3.16.1

JWT — djangorestframework_simplejwt

Swagger — drf-yasg

SQLite

Pytest

Docker

⚙️ Запуск проекта

🔹 Через Docker

git clone https://github.com/the-Hodor/my_library.git

cd my_library-main
docker compose up --build

После запуска сервер будет доступен по адресу:

http://localhost:8000

🔹 Локальный запуск (без Docker)

python -m venv venv

source venv/bin/activate  # Windows: venv\Scripts\activate

pip install -r requirements.txt

python manage.py migrate

python manage.py runserver

📖 Swagger документация

После запуска проекта:

http://localhost:8000/swagger/

Через Swagger можно:

Просматривать все endpoints

Отправлять запросы

Тестировать API без Postman

🔐 JWT-аутентификация

Получить токен

POST запрос:

/api/token/

Body:

{  
 "username": "your_username",  
 "password": "your_password"  
}

Ответ:

{   
  "access": "ACCESS_TOKEN",  
  "refresh": "REFRESH_TOKEN"  
}  

Обновить токен  
/api/token/refresh/  
Использование токена  

Добавить в заголовок запроса:

Authorization: Bearer <access_token>  

📚 API Endpoints

Все endpoints находятся под префиксом:

/api/  

📖 Книги  

Метод	Endpoint	Описание  
GET	   /api/books/	Список книг пользователя  
POST	/api/books/	Создать книгу  
PUT	  /api/update_books/<int:pk>/	Обновить книгу  
POST	/api/mark_read/<int:pk>/	Отметить книгу как прочитанную  

📝 Заметки

Метод	Endpoint	Описание  
POST	/api/create_notes/	Создать заметку  

🧪 Тестирование

Проект использует:

pytest

pytest-django

Запуск тестов:

pytest

Тестируется сервисный слой:

book_service

note_service

🧠 Архитектура проекта

Проект разделён на несколько уровней:

Models

Book

Genre

Note

MyUser (кастомная модель пользователя)

Service Layer

Бизнес-логика вынесена в отдельные модули:

book_service.py

note_service.py

Это позволяет:

Упростить тестирование

Снизить связанность кода

Сделать архитектуру более поддерживаемой

API Layer

DRF views

Serializers

JWT-аутентификация

🐳 Docker

Проект запускается в одном контейнере:

Django backend

SQLite как база данных

👨‍💻 Назначение проекта

Учебный backend-проект для практики:

работы с Django и DRF

JWT-аутентификации

сервисного слоя

тестирования

контейнеризации